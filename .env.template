# Django Settings
SECRET_KEY=django-insecure-your-secret-key-here
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1,*

# Database Settings
# DATABASE_URL não definida aqui de propósito.
# - Dev local (sem Docker): settings.py usa SQLite por padrão
# - Docker: docker-compose.dev.yml define DATABASE_URL via environment:
DB_NAME=almoxarifado
DB_USER=postgres
DB_PASSWORD=postgres
